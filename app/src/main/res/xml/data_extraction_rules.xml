<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- android:allowBackup is false at the manifest level, and we additionally
         exclude everything explicitly so no OEM backup path can ever leak
         profile secrets (private keys, passwords, UUIDs) off the device. -->
    <cloud-backup>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
